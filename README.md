# invoice-generator
Invoice-generator is a Python-based project to generate invoices in PDF format. The project uses the ReportLab module to create and format PDF files. Invoice-generator allows you to create personalized invoices with basic information such as customer details, products/services, amounts, taxes, and a general summary.
